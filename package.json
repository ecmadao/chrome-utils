{
  "name": "chrome-utils",
  "version": "0.0.1",
  "description": "Some tools that help to build chrome extension",
  "main": "dist/index.js",
  "scripts": {
    "build": "babel lib -d dist --copy-files",
    "lint": "./node_modules/eslint/bin/eslint.js lib",
    "test": "echo \"Error: no test specified\" && exit 1",
    "prepublish": "in-publish && npm run build || not-in-publish",
    "tag": "git tag npm-v$npm_package_version",
    "postversion": "git commit package.json -m \"npm-v$npm_package_version\" && npm run tag && git push && git push --tags"
  },
  "keywords": [
    "chrome",
    "chrome-extension"
  ],
  "author": "ecmadao",
  "license": "MIT",
  "dependencies": {
    "babel-core": "^6.18.2",
    "babel-polyfill": "^6.16.0",
    "object-assign": "^4.1.0"
  },
  "devDependencies": {
    "babel-cli": "^6.24.1",
    "babel-plugin-transform-runtime": "^6.15.0",
    "babel-preset-env": "0.0.7",
    "eslint": "^3.9.1",
    "eslint-config-airbnb-base": "^10.0.1",
    "eslint-plugin-import": "^2.2.0",
    "in-publish": "^2.0.0"
  }
}
